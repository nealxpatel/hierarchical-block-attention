# Pinned deps installed on top of pytorch/pytorch:2.10.0-cuda12.8-cudnn9-runtime
# (docker/Dockerfile). torch itself comes from the base image and is
# deliberately NOT listed/reinstalled here -- see docker/README.md.
#
# This file is dual-purpose: it builds the image, AND scripts/provision.sh's
# env-pin assert parses transformers/tokenizers versions straight out of it at
# provisioning time (single source of truth -- no duplicated pin to drift).
# A version bump here means rebake, not a live edit on a running box
# (docker/README.md, "rebake threshold").
transformers==4.57.6
tokenizers==0.22.2
datasets==4.6.1
numpy>=1.26,<3
safetensors>=0.4
huggingface-hub>=0.35
# boto3: the S3-compatible-bucket data-fetch backend used by
# scripts/provision.sh (DATA_ENDPOINT/DATA_BUCKET/DATA_ACCESS_KEY_ID/
# DATA_SECRET_ACCESS_KEY). The other supported backend, rclone (DATA_REMOTE),
# is a system package installed in docker/Dockerfile, not a pip dep.
boto3>=1.34,<2
